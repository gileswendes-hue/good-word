{
  "name": "good-word-game-backend",
  "version": "1.0.0",
  "description": "Backend for GOOD WORD / BAD WORD game using Node.js, Express, and MongoDB.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",  // <-- RENDER NEEDS THIS LINE
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongoose": "^8.0.3"
  }
}
